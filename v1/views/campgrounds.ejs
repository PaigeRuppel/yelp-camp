<%	include partials/header %>

<h1>This is the campground page</h1>

<% campgrounds.forEach(function(campground){ %>
	<div>
		<h4><%= campground.name %><h4>
		<img src="<%= campground.image %>" alt="Image for <%= campground.name %>">
	</div>
<% }); %>

<% include partials/footer %>
